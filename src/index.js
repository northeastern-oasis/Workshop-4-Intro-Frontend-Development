import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// This line tells React to render our app. There's no need to change anything here!
ReactDOM.render(<App />, document.getElementById("root"));
